# IMAP Configuration
IMAP_HOST=imap.example.com
IMAP_PORT=993
IMAP_USER=user@example.com
IMAP_PASSWORD=your-password
IMAP_FOLDER=INBOX
IMAP_SPAM_FOLDER=Spam
IMAP_USE_IDLE=true
IMAP_POLL_INTERVAL=60

# Anthropic API
ANTHROPIC_API_KEY=sk-ant-...
CLAUDE_MODEL=haiku  # haiku (fast/cheap), sonnet (balanced), opus (best)

# Spam Detection
# SPAM_SENSITIVITY: Controls how aggressive spam detection is (1-10 scale)
#   1-3: Very lenient - Only flags obvious spam, minimizes false positives
#        Use if you get many legitimate emails marked as spam (e.g., newsletters, dating sites)
#   4-6: Balanced (default: 5) - Good balance between spam detection and false positives
#        Recommended for most users
#   7-10: Strict - Flags anything suspicious, may cause false positives
#         Use if you prefer safety over convenience
SPAM_SENSITIVITY=5
SPAM_PROMPT=Standard    # "Standard" or custom system prompt
ANALYSIS_LIMIT=50       # Max emails to analyze on startup (0 = unlimited)

# Logging
LOG_LEVEL=INFO
LOG_DIR=logs
LOG_RETENTION_DAYS=3
